/*
 * Distribution Profiles
 *
 * Please use only "dist" or "feature" prefix
 */

configurations {
	distBase {
		transitive = false
		visible = false
	}
}

dependencies {
	distBase project(':modules:privacy-web')
}
